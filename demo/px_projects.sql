create table px_projects (
    id            number        generated by default on null as identity (start with 1) primary key not null
  , name_jtl      varchar2(500) not null constraint px_projects_tl_ck CHECK (name_jtl is json(strict))
  , alias         varchar2(32)
  , active_ind    varchar2(1)   not null
  , created_by    varchar2(60) default 
                    coalesce(
                        sys_context('APEX$SESSION','app_user')
                      , regexp_substr(sys_context('userenv','client_identifier'),'^[^:]*')
                      , sys_context('userenv','session_user')
                    )
                    not null
  , created_on    date         default sysdate not null
  , updated_by    varchar2(60)
  , updated_on    date
  , constraint px_projects_ck_active
      check (active_ind in ('Y', 'N'))
)
enable primary key using index
/

comment on table px_projects is 'List of projects';

comment on column px_projects.name_jtl is 'JSON data with the language as keys. Syntax: [{"l": "us", "tl": "Project Analysis"}]';
comment on column px_projects.active_ind is 'Is the record enabled Y/N?';
comment on column px_projects.created_by is 'User that created this record';
comment on column px_projects.created_on is 'Date the record was first created';
comment on column px_projects.updated_by is 'User that last modified this record';
comment on column px_projects.updated_on is 'Date the record was last modified';


--------------------------------------------------------
--  DDL for Trigger px_projects_u
--------------------------------------------------------
create or replace trigger px_projects_u
before update
on px_projects
referencing old as old new as new
for each row
begin
  :new.updated_on := sysdate;
  :new.updated_by := coalesce(
                         sys_context('APEX$SESSION','app_user')
                       , regexp_substr(sys_context('userenv','client_identifier'),'^[^:]*')
                       , sys_context('userenv','session_user')
                     );
end;
/
alter trigger px_projects_u enable;
